## Packages
date-fns | Date formatting for event cards and lists
framer-motion | Smooth transitions and animations for page loads/modals
lucide-react | Icons for the UI (already in base, but good to note usage)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
